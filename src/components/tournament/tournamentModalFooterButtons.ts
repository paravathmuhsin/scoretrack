/**
 * Footer actions for tournament-style modals (Schedule match, Create group).
 * Pill buttons, stacked full-width on mobile — matches mobile modal reference.
 */
export const tournamentModalFooterPrimaryButtonClass =
  'min-h-[48px] h-12 max-sm:h-14 w-full flex-1 rounded-full px-5 text-base font-semibold !text-primary-foreground shadow-md sm:min-w-0'

export const tournamentModalFooterOutlineButtonClass =
  'min-h-[48px] h-12 max-sm:h-14 w-full flex-1 rounded-full px-5 text-base font-semibold shadow-sm sm:min-w-0'

/** Single primary action row (e.g. AddPlayersModal Done). */
export const tournamentModalFooterSinglePrimaryButtonClass =
  'min-h-[48px] h-12 max-sm:h-14 w-full rounded-full px-5 text-base font-semibold !text-primary-foreground shadow-md'
