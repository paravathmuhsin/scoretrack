# Copy to keystore.properties and fill in after running: npm run cap:android:keystore
# Never commit keystore.properties or *.keystore to git.

storeFile=app/scoretrack-release.keystore
storePassword=your-store-password
keyAlias=scoretrack
keyPassword=your-key-password
